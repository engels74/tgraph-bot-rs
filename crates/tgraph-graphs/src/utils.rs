//! Graph utility functions and helpers.

/// Utility functions for graph generation.
pub struct GraphUtils;

impl GraphUtils {
    /// Calculates optimal graph dimensions.
    pub fn calculate_dimensions(data_points: usize) -> (u32, u32) {
        // Simple calculation - will be improved later
        let width = 800;
        let height = 600;
        (width, height)
    }
}
